import type { ReactElement } from 'react';

export default function NotesPage(): ReactElement {
  return (
    <section>
      <h1>Your notes</h1>
      <p>The notes list and editor are added in a later phase.</p>
    </section>
  );
}
